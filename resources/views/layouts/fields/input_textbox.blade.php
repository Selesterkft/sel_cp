<div class="form-group col-sm-12">
    <label for="{{ $textbox_id }}" class="control-label">{{ $title }}:</label>
    <div class="">
        <input id="{{ $textbox_id }}" name="{{ $textbox_id }}"
               class="form-control" value="{{ $textbox_value }}">
    </div>
</div>
